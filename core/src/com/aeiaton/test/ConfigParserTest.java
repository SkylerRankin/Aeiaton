package com.aeiaton.test;

import static org.junit.Assert.assertEquals;

import java.util.Dictionary;
import java.util.Enumeration;

import org.junit.Test;

import com.aeiaton.util.ConfigParser;

public class ConfigParserTest {
    
    @Test
    public void test() {
        /*
        Dictionary<String, String> dict = ConfigParser.parse("test\\test_menu.config", "title");
        Enumeration<String> keys = dict.keys();
        String[] _keys = {"pad", "width", "height", "font-size"};
        String[] _values = {"pad", "width", "height", "font-size"};
        int i = 0;
        while (keys.hasMoreElements()) {
            String key = keys.nextElement();
            String value = dict.get(key);
            assertEquals(key, _keys[i]);
            assertEquals(value, _values[i]);
            i++;
        }*/
    }

}
